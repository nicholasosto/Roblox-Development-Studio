---
title: "@trembus/rig-spawner"
status: active
updated: 2026-05-01
tags: { lane: ts }
---

# @trembus/rig-spawner

> **Status:** active (2026-05-01)

## Purpose

Rig catalog and spawn service for character rigs.

## Public API

`external-locations/code/roblox-packages-mono/packages/rig-spawner/src/index.ts` is the export surface.

## Dependencies

Live dependency data is generated, not transcribed — see the registry (`previews/dashboards/packages.json`).

## Consumers

See `consumedBy` in the registry. External consumers use `link:../roblox-packages-mono/packages/rig-spawner`.

## Notes

Legacy Flamework-history package — the documented exception noted in [[0002-framework-agnostic-packages]].
