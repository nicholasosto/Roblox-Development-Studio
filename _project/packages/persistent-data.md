---
title: "@trembus/persistent-data"
status: active
updated: 2026-05-01
tags: { lane: ts }
---

# @trembus/persistent-data

> **Status:** active (2026-05-01)

## Purpose

Type-safe player data persistence wrapping `@rbxts/profile-store`.

## Public API

`external-locations/code/roblox-packages-mono/packages/persistent-data/src/index.ts` is the export surface.

## Dependencies

Live dependency data is generated, not transcribed — see the registry (`previews/dashboards/packages.json`).

## Consumers

See `consumedBy` in the registry. External consumers use `link:../roblox-packages-mono/packages/persistent-data`.

## Notes

The foundation tier: the most-depended-upon internal package (currency, stats, both slotable-items variants, pets-and-mounts).
