---
title: "@trembus/timer"
status: active
updated: 2026-05-01
tags: { lane: ts }
---

# @trembus/timer

> **Status:** active (2026-05-01)

## Purpose

Framework-agnostic timer system — state machine, formatting, thresholds, and global signal bus.

## Public API

`external-locations/code/roblox-packages-mono/packages/timer/src/index.ts` is the export surface.

## Dependencies

Live dependency data is generated, not transcribed — see the registry (`previews/dashboards/packages.json`).

## Consumers

See `consumedBy` in the registry. External consumers use `link:../roblox-packages-mono/packages/timer`.

## Notes

Display concerns deliberately split into [[timer-ui]] — the canonical example of the [[0004-one-concern-per-package]] gate.
