---
title: "@trembus/stats"
status: active
updated: 2026-05-01
tags: { lane: ts }
---

# @trembus/stats

> **Status:** active (2026-05-01)

## Purpose

Two-tier stat system — primary attributes, derived stats, resource pools, and persistence slice.

## Public API

`external-locations/code/roblox-packages-mono/packages/stats/src/index.ts` is the export surface.

## Dependencies

Live dependency data is generated, not transcribed — see the registry (`previews/dashboards/packages.json`).

## Consumers

See `consumedBy` in the registry. External consumers use `link:../roblox-packages-mono/packages/stats`.

## Notes

Persistence integration is optional (`peerDependenciesMeta`), per [[0003-peer-dependency-consumption-model]].
