---
title: "@trembus/slotable-items"
status: active
updated: 2026-05-01
tags: { lane: ts }
---

# @trembus/slotable-items

> **Status:** active (2026-05-01)

## Purpose

Unified RPG inventory system — item types, slot management, loadouts, and network operations.

## Public API

`external-locations/code/roblox-packages-mono/packages/slotable-items/src/index.ts` is the export surface.

## Dependencies

Live dependency data is generated, not transcribed — see the registry (`previews/dashboards/packages.json`).

## Consumers

See `consumedBy` in the registry. External consumers use `link:../roblox-packages-mono/packages/slotable-items`.

## Notes

Stays `active` until the [[slotable-items-cutover]] pipeline ships, then flips to `superseded` by [[slotable-items-dissolved]] per [[0005-open-taxonomy-slotable-items]].
